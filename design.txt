## Main loop

while video
    read video frame
    if vehicles detected
        merge to tracking list
    for all tracked vehicles
        calculate distance
        display distance

## Known issues

* The bounding box size does not change with the distance.  The vehicle needs
probably to be extracted from the background to improve this.
